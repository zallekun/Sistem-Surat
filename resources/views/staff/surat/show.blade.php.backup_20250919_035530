@extends('layouts.app')

@section('title', 'Detail Surat')

@section('content')
<div class="py-12">
    <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
        <div class="bg-white overflow-hidden shadow-sm sm:rounded-lg">
            <div class="p-6 bg-white border-b border-gray-200">
                <h1 class="text-2xl font-semibold mb-6">Detail Surat: {{ $surat->nomor_surat }}</h1>

                

                

                <!-- Stepper/Progress for Status -->
                <div class="mb-8">
                    <h2 class="text-xl font-semibold mb-4">Status Perjalanan Surat</h2>
                    <div class="flex justify-between gap-x-4 overflow-x-auto whitespace-nowrap">
                        @foreach($allStatuses as $statusOption)
                            @php
                                $isActive = $surat->currentStatus->urutan >= $statusOption->urutan;
                                $isCurrent = $surat->currentStatus->id === $statusOption->id;
                                $statusClass = $isActive ? 'bg-blue-500 text-white' : 'bg-gray-200 text-gray-600';
                            @endphp
                            <div class="flex-shrink-0 w-32 text-center">
                                <div class="w-8 h-8 mx-auto rounded-full flex items-center justify-center font-medium {{ $statusClass }} text-xs font-bold">
                                    {{ $statusOption->urutan }}
                                </div>
                                <p class="mt-2 text-sm text-center {{ $isCurrent ? 'font-bold text-blue-700' : 'text-gray-600' }}">{{ $statusOption->nama_status }}</p>
                            </div>
                            @if(!$loop->last)
                                <div class="flex-1 h-0.5 {{ $isActive ? 'bg-blue-500' : 'bg-gray-200' }}"></div>
                            @endif
                        @endforeach
                    </div>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                    <!-- Informasi Utama -->
                    <div class="p-4 border rounded-lg shadow-sm">
                        <h2 class="text-xl font-semibold mb-4 border-b pb-2">Informasi Utama</h2>
                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <p class="text-sm font-medium text-gray-500">Nomor Surat:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->nomor_surat }}</p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Perihal:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->perihal }}</p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Tanggal Surat:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->tanggal_surat->format('d F Y') }}</p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Lampiran:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->lampiran ?? '-' }}</p>
                            </div>
                        </div>
                    </div>

                    <!-- Informasi Pembuatan -->
                    <div class="p-4 border rounded-lg shadow-sm">
                        <h2 class="text-xl font-semibold mb-4 border-b pb-2">Informasi Pembuatan</h2>
                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <p class="text-sm font-medium text-gray-500">Dibuat Oleh:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->createdBy?->nama ?? 'N/A' }}</p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Jabatan Pembuat:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->createdBy?->jabatan?->nama_jabatan ?? 'N/A' }}</p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Prodi Pembuat:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->createdBy?->prodi?->nama_prodi ?? 'N/A' }}</p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Fakultas Pembuat:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->createdBy?->prodi?->fakultas?->nama_fakultas ?? 'N/A' }}</p>
                            </div>
                        </div>
                    </div>

                    <!-- Informasi Tujuan & Status -->
                    <div class="p-4 border rounded-lg shadow-sm lg:col-span-2">
                        <h2 class="text-xl font-semibold mb-4 border-b pb-2">Tujuan & Status Surat</h2>
                        <div class="grid grid-cols-2 gap-4">
                            <div>
                                <p class="text-sm font-medium text-gray-500">Tujuan Jabatan:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->tujuanJabatan?->nama_jabatan ?? 'N/A' }}</p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Sifat Surat:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->sifat_surat }}</p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Status Saat Ini:</p>
                                <p class="mt-1 text-lg text-gray-900">
                                    <span class="px-2 py-1 text-xs leading-5 font-semibold rounded-full whitespace-nowrap" style="background-color: {{ $surat->currentStatus->warna_status ?? '#a0aec0' }}; color: {{ \App\Helpers\StatusHelper::getTextColor($surat->currentStatus->warna_status ?? '#a0aec0') }}">
                                        {{ $surat->currentStatus?->nama_status ?? 'N/A' }}
                                    </span>
                                </p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Tanggal Dibuat:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->created_at->format('d F Y H:i') }}</p>
                            </div>
                            <div>
                                <p class="text-sm font-medium text-gray-500">Terakhir Diperbarui:</p>
                                <p class="mt-1 text-lg text-gray-900">{{ $surat->updated_at->format('d F Y H:i') }}</p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- File Surat -->
                <div class="mt-8 p-4 border rounded-lg shadow-sm">
                    <h2 class="text-xl font-semibold mb-4 border-b pb-2">File Surat</h2>
                    @if($surat->file_surat)
                        <p class="text-sm font-medium text-gray-500">Nama File:</p>
                        <p class="mt-1 text-lg text-gray-900">{{ basename($surat->file_surat) }}</p>
                        <p class="text-sm font-medium text-gray-500 mt-2">Ukuran File:</p>
                        <p class="mt-1 text-lg text-gray-900">{{ Storage::disk('public')->exists($surat->file_surat) ? round(Storage::disk('public')->size($surat->file_surat) / 1024, 2) . ' KB' : 'N/A' }}</p>
                        <p class="text-sm font-medium text-gray-500 mt-2">Versi Surat:</p>
                        <p class="mt-1 text-lg text-gray-900">{{ preg_match('/([a-z])$/i', $surat->nomor_surat, $matches) ? 'Revisi ' . strtoupper($matches[1]) : 'Asli' }}</p>
                        
                        <a href="{{ Storage::url($surat->file_surat) }}" target="_blank" class="inline-flex items-center px-4 py-2 bg-blue-600 border border-transparent rounded-md font-semibold text-xs text-white uppercase tracking-widest hover:bg-blue-700 active:bg-blue-900 focus:outline-none focus:border-blue-900 focus:ring ring-blue-300 disabled:opacity-25 transition ease-in-out duration-150 mt-4">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path></svg>
                            Lihat/Unduh PDF
                        </a>
                        <button onclick="window.print()" class="inline-flex items-center px-4 py-2 bg-gray-600 border border-transparent rounded-md font-semibold text-xs text-white uppercase tracking-widest hover:bg-gray-700 active:bg-gray-900 focus:outline-none focus:border-gray-900 focus:ring ring-gray-300 disabled:opacity-25 transition ease-in-out duration-150 mt-4 ml-2">
                            <i class="fa-solid fa-print mr-2"></i> Cetak
                        </button>
                    @else
                        <p class="text-gray-600">Tidak ada file PDF yang diunggah.</p>
                    @endif
                </div>

                <!-- Perjalanan Surat (Tracking) -->
                <div class="mt-8 p-4 border rounded-lg shadow-sm">
                    <h2 class="text-xl font-semibold mb-4 border-b pb-2">Perjalanan Surat (Tracking)</h2>
                    @if($surat->tracking->count() > 0)
                        <div class="relative border-l-2 border-gray-200 ml-3">
                            @foreach($surat->tracking->sortBy('created_at') as $track)
                                <div class="mb-8 ml-6">
                                    <span class="absolute flex items-center justify-center w-6 h-6 bg-blue-100 rounded-full -left-3 ring-8 ring-white">
                                        <i class="fa-solid fa-check text-blue-600"></i>
                                    </span>
                                    <div class="p-4 bg-white border border-gray-200 rounded-lg shadow-sm">
                                        <div class="items-center justify-between mb-3 sm:flex">
                                            <time class="mb-1 text-xs font-normal text-gray-400 sm:order-last sm:mb-0">{{ $track->created_at->format('d F Y, H:i') }}</time>
                                            <div class="text-sm font-normal text-gray-500 lex">
                                                <span class="font-semibold text-gray-900">{{ $track->user->nama ?? 'Sistem' }}</span>
                                                <span class="bg-gray-100 text-gray-800 text-xs font-medium px-2.5 py-0.5 rounded">{{ $track->user->jabatan->nama_jabatan ?? 'N/A' }}</span>
                                            </div>
                                        </div>
                                        <div class="p-3 text-xs italic font-normal text-gray-500 border border-gray-200 rounded-lg bg-gray-50">
                                            {{ $track->keterangan }}
                                        </div>
                                    </div>
                                </div>
                            @endforeach
                        </div>
                    @else
                        <p class="text-gray-600">Belum ada riwayat perjalanan surat.</p>
                    @endif
                </div>

                <!-- Aksi Tambahan -->
                <div class="mt-8 p-4 border rounded-lg shadow-sm">
                    <h2 class="text-xl font-semibold mb-4 border-b pb-2">Aksi</h2>
                <div class="flex flex-wrap gap-2 mt-3">
                    {{-- Edit button for draft --}}
                    @if($surat->currentStatus->kode_status === 'draft' && $surat->created_by === Auth::id())
                    <a href="{{ route('staff.surat.edit', $surat->id) }}" 
                       class="inline-flex items-center px-4 py-2 bg-yellow-100 text-yellow-700 rounded-lg hover:bg-yellow-200 transition-colors">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
                        </svg>
                        Edit Surat
                    </a>
                    
                    {{-- Submit button for draft --}}
                    <form action="{{ route('surat.submit', $surat->id) }}" method="POST" class="inline-block">
                        @csrf
                        <button type="submit" 
                                onclick="return confirm('Kirim surat ini ke Kaprodi untuk review?')"
                                class="inline-flex items-center px-4 py-2 bg-blue-100 text-blue-700 rounded-lg hover:bg-blue-200 transition-colors">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"/>
                            </svg>
                            Kirim ke Kaprodi
                        </button>
                    </form>
                    @endif
                    
                    {{-- Approve/Reject for kaprodi --}}
                    @if(Auth::user()->hasRole('kaprodi') && in_array($surat->currentStatus->kode_status, ['review_kaprodi', 'diajukan']))
                    <form action="{{ route('surat.approve', $surat->id) }}" method="POST" class="inline-block">
                        @csrf
                        <button type="submit" 
                                onclick="return confirm('Setujui surat ini?')"
                                class="inline-flex items-center px-4 py-2 bg-green-100 text-green-700 rounded-lg hover:bg-green-200 transition-colors">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                            </svg>
                            Setujui
                        </button>
                    </form>
                    
                    <button onclick="showRejectModal()" 
                            class="inline-flex items-center px-4 py-2 bg-red-100 text-red-700 rounded-lg hover:bg-red-200 transition-colors">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                        </svg>
                        Tolak
                    </button>
                    @endif
                    
                    {{-- Print button --}}
                    <button onclick="window.print()" 
                            class="inline-flex items-center px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2z"/>
                        </svg>
                        Cetak
                    </button>
                    
                    {{-- Back button --}}
                    <a href="{{ route('surat.index') }}" 
                       class="inline-flex items-center px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                        </svg>
                        Kembali
                    </a>
                </div></div>
                </div>

                <div class="mt-6">
                    <a href="{{ route('staff.surat.index') }}" class="inline-flex items-center px-4 py-2 bg-gray-200 border border-transparent rounded-md font-semibold text-xs text-gray-800 uppercase tracking-widest hover:bg-gray-300 focus:outline-none focus:border-gray-900 focus:ring ring-gray-300 disabled:opacity-25 transition ease-in-out duration-150">
                        Kembali ke Daftar Surat
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection